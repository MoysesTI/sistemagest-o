# ==========================================
# VARIÁVEIS DE AMBIENTE - PRODUÇÃO
# ==========================================
# Copie este arquivo para .env e configure os valores

# Servidor
NODE_ENV=production
PORT=5001

# Banco de Dados PostgreSQL
# Para Render/Railway, use a URL fornecida pelo serviço
DATABASE_URL=postgresql://usuario:senha@host:5432/database?schema=public

# Segurança - JWT (OBRIGATÓRIO trocar em produção!)
# Gere um novo com: node -e "console.log(require('crypto').randomBytes(64).toString('hex'))"
JWT_SECRET=1ef8917aae9215a11fc33b4d256eb301fe52cefa56052271375ba6f4a2578eaf344ad88a09fc8b60c5104064fd612f22115191b0d080850478e7e7f869de83e0
JWT_EXPIRES_IN=7d

# CORS - Origens permitidas (separadas por vírgula)
ALLOWED_ORIGINS=https://seu-dominio.com,https://www.seu-dominio.com

# Admin padrão (criado na primeira execução)
ADMIN_EMAIL=admin@prismatech.com
ADMIN_PASSWORD=SenhaForte2024!

# Configurações de negócio
VALOR_HORA_AULA=27.00
